<project name="cTomLabViewer" default="build_zip" basedir=".">
	<property file="build-download.properties" />

	<!--
	TARGET: Zip
		Zip the source code for cTomDataPersistence, cTomLabViewer,  LabLoader, RegistrationConsumerService, StudyConsumerService, StudyLookUpService
	-->

	<target name="build_zip">
		<mkdir dir="${dest.dir}" />
		<zip destfile="${dest.dir}/${app.name}.zip">
			<zipfileset dir="${artifacts.dir}" prefix="${app.name}/artifacts"/>
			<zipfileset dir="${cTomData.dir}" prefix="${app.name}/CTOMDataPersistence">
				<exclude name="**/bin/**"/>
			</zipfileset>
			<zipfileset dir="${cTomLab.dir}" prefix="${app.name}/ctomlabviewer">
				<exclude name="**/build/**"/>
			</zipfileset>
			<zipfileset dir="${labLoader.dir}" prefix="${app.name}/LabLoader">
				<exclude name="**/build/**"/>
				<exclude name="**/bin/**"/>
			</zipfileset>
			<zipfileset dir="${regConsumerService.dir}" prefix="${app.name}/RegistrationConsumerService">
				<exclude name="**/build/**"/>
				<exclude name="**/bin/**"/>
			</zipfileset>
			<zipfileset dir="${studyConsumerService.dir}" prefix="${app.name}/StudyConsumerService">
				<exclude name="**/build/**"/>
				<exclude name="**/bin/**"/>
			</zipfileset>
			<zipfileset dir="${studyLookupService.dir}" prefix="${app.name}/StudyLookUpService">
				<exclude name="**/build/**"/>
				<exclude name="**/bin/**"/>
			</zipfileset>
			<zipfileset dir="${ddl.dir}" prefix="${app.name}/DDL"/>
		</zip>
	</target>

</project>
